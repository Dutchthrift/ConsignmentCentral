<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Dutch Thrift</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    
    .login-container {
      background: white;
      padding: 3rem;
      border-radius: 12px;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 400px;
      text-align: center;
    }
    
    .logo {
      font-size: 2rem;
      font-weight: bold;
      color: #333;
      margin-bottom: 0.5rem;
    }
    
    .subtitle {
      color: #666;
      margin-bottom: 2rem;
    }
    
    .access-buttons {
      display: grid;
      gap: 1rem;
      margin-bottom: 2rem;
    }
    
    .btn {
      display: block;
      padding: 1rem 1.5rem;
      border: none;
      border-radius: 8px;
      font-size: 1rem;
      font-weight: 600;
      text-decoration: none;
      cursor: pointer;
      transition: all 0.3s ease;
    }
    
    .btn-admin {
      background: #3498db;
      color: white;
    }
    
    .btn-admin:hover {
      background: #2980b9;
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(52, 152, 219, 0.3);
    }
    
    .btn-consignor {
      background: #2ecc71;
      color: white;
    }
    
    .btn-consignor:hover {
      background: #27ae60;
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(46, 204, 113, 0.3);
    }
    
    .help-text {
      color: #888;
      font-size: 0.9rem;
      line-height: 1.5;
    }
    
    .help-text strong {
      color: #555;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <div class="logo">Dutch Thrift</div>
    <div class="subtitle">Choose your access level</div>
    
    <div class="access-buttons">
      <a href="/direct/admin-access" class="btn btn-admin">
        Login as Admin
      </a>
      
      <a href="/direct/consignor-access" class="btn btn-consignor">
        Login as Consignor
      </a>
    </div>
    
    <div class="help-text">
      <p><strong>Admin:</strong> Full platform management</p>
      <p><strong>Consignor:</strong> Item management & sales tracking</p>
    </div>
  </div>
</body>
</html>