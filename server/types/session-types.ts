/**
 * Session type definitions for the authentication system
 */

// User types for session storage
export type UserTypeValue = 'admin' | 'consignor';

// User roles for access control
export type UserRoleValue = 'admin' | 'consignor';